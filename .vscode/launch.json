{
    // Use IntelliSense to learn about possible attributes.
    // Hover to view descriptions of existing attributes.
    // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "001main",
            "type": "go", // 需要在 vscode 中安装 go 扩展
            "request": "launch",
            "mode": "auto",
            "env": {
                "PATH": "$PATH:${workspaceFolder}/bin",
                "GOROOT": "${workspaceFolder}",
                "GOPATH": "${workspaceFolder}/gopath",
            },
            "program": "${workspaceFolder}/gopath/src/001.println",
            "args": [],
            "comment": "放弃使用 vscode, 只能使用 gdb 手动调试, 因为在 v1.2 还没有 delve 工具, 现在的 delve 工具也不兼容 v1.2 的 golang"
        }
    ]
}
